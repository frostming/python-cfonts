language: python
sudo: false

install:
  - python -m pip install -U "pdm>=0.2.1"
  - pdm install -d

script:
  - pdm run flake8 --ignore=W,E203,F405 --max-line-length=88 cfonts tests
  - pdm run pytest tests

jobs:
  include:
    - python: "3.7"
      dist: xenial
      sudo: required
    - python: "3.6"
    - python: "3.5"
    - python: "2.7"

notifications:
  email: false
